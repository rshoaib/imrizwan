# imrizwan.com SEO Audit — 2026-05-07

**Site:** https://imrizwan.com
**Auditor:** Automated SEO Audit (scheduled task `imrizwan-gsc-audit-fixes`)
**Run mode:** Technical-SEO only (GSC connector not available this run)
**Stack:** Next.js 16 App Router on Vercel, Markdown blog posts (gray-matter)

---

## Summary

- Total impressions (7d): _unavailable — see "GSC unavailable" below_
- Total clicks (7d): _unavailable_
- Site-wide CTR: _unavailable_
- Avg position: _unavailable_
- Technical-SEO health this run: **green** — `validate-seo.js` passes, all sampled URLs return 200 with correct canonicals, all internal `/blog/...` links resolve, all 39 posts have valid frontmatter (image, excerpt ≥80 chars), no raw `<img>` without alt outside code fences, no body H1s.
- New issues this week: 1 build/lint warning (no viewport in root layout), and a non-SEO operational issue with the local repo (see "Operational blockers").

## GSC unavailable

No Google Search Console connector is exposed to this scheduled-task runner. Per the task's hard rule ("Never fabricate GSC metrics. If the connector isn't available, say so and do the technical-SEO part of the audit only"), all metric-driven sections (top queries, low-CTR rewrites, striking-distance queries, indexing anomalies, Core Web Vitals) are deferred to a run where GSC is connected.

A `.gsc-service-account.json` file exists in the repo root (untracked, mode 600) but no scripts are wired up to use it. If you want to unblock automated GSC pulls, either:
1. Connect a GSC MCP connector to the Cowork session that runs this task, OR
2. Add a Node script (e.g. `scripts/gsc-pull.js`) that uses the existing service-account file via `googleapis` and writes the last-7-day query/page exports to `.cache/gsc/YYYY-MM-DD.json` for this task to consume. The service account email will need to be granted `Restricted` user access in the GSC property.

## Auto-fixes applied

**None.** Auto-fixes were skipped this run because of an operational blocker on the local repo (see next section). Applying edits without being able to commit/push would only add to the existing pile of uncommitted changes.

## Operational blockers (must be resolved by hand)

`C:\Projects\imrizwan` is in a stale, conflicted state that no automated run can recover from:

1. **Stale git index locks** — `.git/index.lock` (May 6) and `.git/index.stash.3.lock` (Apr 30) are owned by a Windows-side process and cannot be `unlink`'d from the bash sandbox (`rm: Operation not permitted`). This blocks every git write op (`add`, `commit`, `stash`, `pull --rebase`). Documented in `feedback_globals_css_edit_truncation.md`; the workaround is a PowerShell script triggered via the Run dialog, which a scheduled task can't drive unattended.

2. **Local commits and modifications duplicate origin** — local is "ahead 1 / behind ~10" after `git fetch`. The "ahead" commit (`bc96162` Microsoft Graph Throttling) is already on origin under a different SHA (`a3cb696`). All 11 `M` files (refreshes of viva-connections, graph-api-auth, expressions cheat sheet, etc.) overlap with refresh commits already on origin. Both untracked .md posts in `content/blog/` (`microsoft-graph-batch-requests-...` and `spfx-form-customizer-extensions-...`) already exist on origin. So nothing local is uniquely valuable.

3. **Cruft files** — 3 `.evict`/`.tmp`/`.staging` artifacts dating from Apr 25–28 (sharepoint-column-formatting, migrate-yeoman-to-new-spfx-cli, seo-audit-2026-04-24) appear to be partial-write fallbacks from prior failed runs. Plus loose helper scripts at repo root (`_fix-deploy.ps1`, `_pipeline-prep.ps1`, `_publish-batch-post.ps1`, `foo.txt`) and an untracked `.git-quarantine/` directory.

**Recommended one-time recovery (run on Windows side, with a clean working tree backup first):**
```powershell
# from PowerShell, in C:\Projects\imrizwan
Remove-Item .git\index.lock, .git\index.stash.3.lock, .git\index.stash.*.lock -ErrorAction SilentlyContinue
Remove-Item .git-quarantine -Recurse -Force -ErrorAction SilentlyContinue
git fetch origin
git reset --hard origin/main
# review and remove cruft:
Remove-Item content\blog\*.evict, content\blog\*.tmp, seo-reports\*.evict, seo-reports\*.staging, foo.txt -ErrorAction SilentlyContinue
git status   # should be clean
```
After this, future scheduled runs of this task can commit/push normally.

## Striking-distance queries (pos 11–20)

Deferred — requires GSC.

## Low-CTR pages rewritten

Deferred — requires GSC.

## Technical-SEO findings (independent of GSC)

### `validate-seo.js`

```
🔍 SEO Validation Pipeline
📄 Scanning 22 pages...
📐 Scanning 1 layouts...
  ⚠ app/layout.tsx: Root layout missing viewport configuration
─── Results ───
  ✓ 22 checks passed
  ⚠ 1 warnings
Pipeline: SEO validation PASSED with warnings
```

Same single warning as last week — **no regression**.

### Public assets

| Asset | Path | Status | Note |
|---|---|---|---|
| robots.txt | `public/robots.txt` | OK (1.1 KB) | Disallows `/api/`, `/_next/`, points to sitemap |
| RSS | `public/rss.xml` | OK (19 KB, Apr 20) | Static fallback; runtime is generated by `scripts/generate-feeds.js` |
| llms.txt | `public/llms.txt` | OK (1.2 KB) | |
| sitemap (static) | `public/sitemap.xml` | Stale (Mar 8) | Live `app/sitemap.ts` is dynamic and current — `lastmod=2026-05-06T13:44Z` confirmed via `curl` |

### Live sitemap vs local posts

| Source | Count |
|---|---|
| Live sitemap blog entries | 42 |
| Local `content/blog/*.md` files (excluding `.tmp`/`.evict`) | 39 |

Three slugs are on the live site but missing from local (because origin has commits this sandbox can't pull): `azure-static-web-apps-microsoft-graph-2026`, `microsoft-graph-powershell-sdk-guide-2026`, `microsoft-graph-sites-selected-granular-sharepoint-permissions-2026`. Will reconcile after the recovery step above.

### Internal `/blog/...` link audit

133 internal `/blog/...` references across all posts — **all resolve to existing posts**. Earlier passes flagged 30 broken refs; those were entirely false positives (28 from `/images/blog/...` paths, 2 from external URLs to `voitanos.io/blog/...`). The detector now uses two strict patterns: `](/blog/<slug>)` and `href="/blog/<slug>"`, which produced zero broken references this run.

### Frontmatter integrity (39 posts)

| Check | Result |
|---|---|
| Posts missing `image` frontmatter | 0 |
| Posts missing `excerpt` frontmatter | 0 |
| Posts with `excerpt` shorter than 80 chars | 0 |
| Posts with raw `<img>` lacking `alt` (outside code fences) | 0 |
| Posts with `![](path)` empty markdown alt | 0 |
| Posts with body-level `# H1` headings | 0 (the 10 candidates were all PowerShell/bash comments inside fenced code blocks) |

### Random sample HTTP HEAD checks

| URL | Status | Canonical |
|---|---|---|
| `/` | 200 | — |
| `/blog` | 200 | — |
| `/about` | 200 | — |
| `/contact` | 200 | — |
| `/blog/microsoft-graph-api-authentication-guide` | 200 | matches |
| `/blog/power-automate-expressions-cheat-sheet-2026` | 200 | — |
| `/blog/sharepoint-online-permissions-complete-guide` | 200 | — |
| `/blog/spfx-migrate-gulp-heft-webpack-2026` | 200 | — |
| `/blog/microsoft-graph-throttling-survive-429-retry-backoff-2026` | 200 | — |

Sample canonical confirmed on graph-api-auth post: `<link rel="canonical" href="https://imrizwan.com/blog/microsoft-graph-api-authentication-guide"/>` and `<title>Master Graph API Authentication | ImRizwan</title>`.

### External link spot-check (top 7 cited)

| URL | Status |
|---|---|
| https://github.com/pnp/List-Formatting | 200 |
| https://jobera.com/sharepoint-statistics/ | 200 |
| https://developer.microsoft.com/json-schemas/sp/v2/column-formatting.schema.json | 200 |
| https://learn.microsoft.com/en-us/sharepoint/dev/embedded/whats-new | 200 |
| https://lists.handsontek.net/microsoft-lists-updates-2025-deprecations-new-features/ | 200 |
| https://developer.microsoft.com/json-schemas/sp/column-formatting.schema.json | 200 |
| https://portal.azure.com | 403 (expected — auth-gated) |

No 404s in the top-cited external set.

## Flagged for human review

- **Repo recovery** — see "Operational blockers". This is the highest-priority item; future automated runs of *every* `imrizwan-*` scheduled task are silently failing to commit until this is resolved.
- **Root layout viewport** — `app/layout.tsx` is missing the `viewport` export. Add:
  ```ts
  export const viewport: Viewport = { width: 'device-width', initialScale: 1 }
  ```
  Low risk, but it's the only `validate-seo.js` warning and it's been there for at least a week.
- **`public/sitemap.xml` staleness** — the static fallback hasn't been regenerated since Mar 8. The dynamic `app/sitemap.ts` is what crawlers actually fetch (and it's current), but the static file's existence is misleading; consider deleting it or wiring a build step that overwrites it.
- **Core Web Vitals / Page Experience / Indexing anomalies / Striking-distance queries** — all deferred to a GSC-connected run.
- **No proposed redirects** this week.

## Next week

1. **Resolve the repo lock + reset** (recovery snippet above) so subsequent runs of this task and the sister content-pipeline/refresh tasks can actually push.
2. **Connect a GSC source** (MCP connector or wire up the existing `.gsc-service-account.json`) so this task can do its actual job (low-CTR rewrites, striking-distance queries, indexing fixes).
3. **Add the `viewport` export** to `app/layout.tsx` to clear the sole `validate-seo.js` warning.

---

_Files touched this run: 1 (this report). Files inside the 15-file budget: 14 remaining (unused — auto-fixes deferred)._
