# imrizwan.com SEO Audit — 2026-04-24

## Summary

- **GSC connector:** Not available in this run — no Google Search Console MCP is connected. Per the task's "Never fabricate GSC metrics" rule, the performance, indexing, Core Web Vitals, and striking-distance sections below are empty and flagged for human follow-up.
- **Technical SEO posture:** Healthy. `validate-seo.js` reports 22/22 checks passed, 1 pre-existing warning (root layout viewport config), no regressions vs. last week.
- **Posts in corpus:** 33 Markdown posts in `content/blog/`.
- **Public artifacts:** `public/robots.txt`, `public/rss.xml`, `public/sitemap.xml`, `public/llms.txt` all present.
- **New issues this week:** 1 broken internal link (fixed), 1 duplicate-H1 in body (fixed).

## Auto-fixes applied

| Category | File | Change | Commit |
|---|---|---|---|
| (b) Broken internal links | `content/blog/sharepoint-column-formatting-guide.md` | Removed stray `.md` suffix in link to `/blog/microsoft-lists-json-formatting-complete-guide-2026` (link was pointing to a non-existent `.md`-suffixed slug, returning 404) | see commit `seo: fix broken internal links` |
| (extra) Duplicate H1 demotion | `content/blog/migrate-yeoman-to-new-spfx-cli.md` | First body heading was an `<h1>` duplicating the page title → demoted to `<h2>` to keep one H1 per page | see commit `seo: demote duplicate body H1 to H2` |

Total files touched: 2. Well under the 15-file cap.

Categories NOT triggered this run:

- **(a) Low-CTR rewrites** — GSC not connected, so no CTR data. Skipped per "never fabricate GSC metrics" rule.
- **(c) Broken external links** — HEAD-checked the 10 most-cited external URLs. All returned 200 except `portal.azure.com` and `entra.microsoft.com` which return 403 (auth-gated admin portals, not broken).
- **(d) Missing alt text** — No Markdown `![](…)` images with empty alt found. Two raw `<img>` mentions are literal code examples, not rendered images.
- **(e) Missing/too-short excerpts** — All 33 posts have excerpts ≥ 80 chars. Minimum observed is 120.
- **(f) Indexing hints for orphans** — Needs GSC "Discovered — currently not indexed" list; skipped.

## Striking-distance queries (pos 11–20)

Not available — requires GSC connector. Flag for next run once connector is installed.

## Low-CTR pages rewritten

Not available — requires GSC connector. Flag for next run once connector is installed.

## Flagged for human review

### Title length over 60 chars (2 posts)

Both are length-only issues, not CTR-driven. I did NOT auto-rewrite because the task's rules scope title rewrites to "the top 5 posts with CTR < 1% and impressions > 100" — a GSC-gated condition. Without CTR data I can't tell whether these are actually underperforming, and a blind rewrite risks replacing a working title.

- `microsoft-graph-toolkit-web-apps-guide-2026.md` — 82 chars: *"Microsoft Graph Toolkit: Build M365-Powered Web Apps with Zero Backend"*
- `power-platform-custom-connectors-rest-api-guide-2026.md` — 73 chars: *"Building Custom Connectors in Power Platform: Connect Any REST API (2026)"*

### Excerpt length over 160 chars (2 posts)

Same 2 posts as above (184 and 176 chars). Same reasoning — deferred to the CTR-gated rewrite pass.

### Core Web Vitals

Not available — requires GSC / PageSpeed data. Flag for human review once connector is installed.

### Indexing anomalies

Not available — requires GSC Coverage report. Flag for human review.

### Proposed redirects

None this week.

### Pre-existing warning from `validate-seo.js`

- `app/layout.tsx`: Root layout missing explicit `viewport` configuration. This warning has existed across multiple weekly runs. Next.js 16's `viewport` export is the supported fix; propose a small PR adding:

  ```ts
  export const viewport = { width: 'device-width', initialScale: 1 }
  ```

  Not auto-fixed because it edits a root layout file and deserves a human reviewer.

## Next week

1. **Install a Google Search Console MCP connector** so the next run can actually pull CTR, impressions, Coverage, and Core Web Vitals data. Every gated section above (low-CTR rewrites, striking-distance queries, indexing hints, CWV flags) depends on it.
2. **Decide on the two long-title posts** (Graph Toolkit, Custom Connectors) — either shorten them now or wait until GSC data confirms they're underperforming.
3. **Add `viewport` export to `app/layout.tsx`** to clear the long-standing `validate-seo.js` warning.
